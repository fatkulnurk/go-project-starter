linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - ineffassign
    - unused
    - revive
    - gocritic

linters-settings:
  revive:
    rules:
      - name: exported

run:
  timeout: 5m
