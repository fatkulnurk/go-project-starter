// Package homepage is the module behind the public landing page at /. It has
// no use cases or data of its own: it only renders a static welcome page from
// the shared view layout. The domain, application, and infrastructure folders
// exist to keep the module skeleton uniform but are intentionally empty.
package homepage
