package homepage

// API is the public surface of the homepage module. Other modules may depend
// only on this, never on the module's internals. The homepage has no use cases
// other modules call, so the API carries no commands or queries today.
type API struct{}
