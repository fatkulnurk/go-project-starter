// Package domain is the homepage module's domain layer. The homepage renders a
// static page and owns no entities, value objects, or repository contracts, so
// this package is intentionally empty. It exists only to keep the module
// skeleton uniform across internal/modules.
package domain
